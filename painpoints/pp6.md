更改当前skills和全局system prompt的引用地址
或者我换个说法吧，让当前代码，全面接受~/.claude中的配置和东西
让当前mcp的配置也和.claude中的mcp接轨，你明白我意思吗，不需要去所谓的～/.zcode
就直接集成claude code的那一套配置
另外我不想/skills去找skill，而是让/mykill 默认显示在和/skill同一级，可以这样理解，让.claude/skills中我的skills，默认作为zcode的根指令
甚至你可以去掉原生的/skill, 因为我的skill默认就在第一层了