node_modules/
dist/
dist-release/
out/
prebuilds/
bundled-resources/
mock-cdn/
*.tsbuildinfo
.DS_Store
coverage/
.env.local
.env.*.local
.env
.VSCodeCounter
.idea/
.zed/
.cache
# CI 会跨任务保留这些下载缓存；不应进入 Git 未跟踪源码枚举。
/.electron-builder-cache/
/.electron-cache/
/.pnpm-store/
/.npm-cache/
.turbo/
z-work/
openclaw/
.run-logs/
.worktrees/
packages/stream-animate/dist-server/
packages/stream-animate/.stream-animate-data/
.codegraph
.mimosa/
# 集成分支曾混入 node-gyp 构建产物与 .playwright-mcp 临时快照；
# 入库由 scripts/ci/ci-repo-hygiene.mjs 在 CI 第一站拒绝，这里让本地 git add 也忽略它们。
.playwright-mcp/
*.o
.deps/
obj.target/
config.gypi
gyp-mac-tool
*.target.mk
binding.Makefile
# 这两条原来写成 build/Makefile、build/Release/*.node，含中间斜杠的 pattern
# 只相对 .gitignore 所在目录（仓库根）生效，匹配不到 packages/ax-macos/build/ 下的产物；
# 而 ci-repo-hygiene.mjs 的 denylist 用的是 (^|/)build/... 任意层级匹配。
# 宽严不一致时无斜杠 pattern 能拦住的产物会被 git add -A 重新扫进索引。
# 加 **/ 让忽略范围与门禁范围对齐。
**/build/Makefile
**/build/Release/*.node

# CUA Helper SEA 基座下载缓存（scripts/cua-helper-sea-base.mjs 落盘的官方 Node 22 运行时，纯下载产物）
/.tmp/
# Python 字节码（docker/plugin-eval/summarize.py 在主机上跑会生成）。
__pycache__/
*.pyc

.zcode-runtime/

# 本地痛点记录，仅用于个人拆解流程，不入库。
painpoints/

# 本地化资源里「第三方通用包」的落点。判据见 third-party/resources.json 的 vendorCriterion：
# 智谱自家的远程资源在本地留一份；第三方通用包直接下载即可、不保留。
#   node/              —— nodejs.org 的 Node 运行时（336 MiB，跑 pnpm/CLI 本就必装）
#   native-search-src/ —— codeload / sourceware 的上游源码包（仅 pnpm build:native-search 用）
# 这些不入库，落点因此排除在索引外，避免误提交。
#
# 注意方向性：third-party/vendored/ 下**唯一**需要入库的是 zhipu-official-plugin/，
# 它必须入库、且不得被任何 .gitignore 命中（由 `node scripts/vendor-resources.mjs assert` 把关）。
third-party/vendored/node/
third-party/vendored/native-search-src/
