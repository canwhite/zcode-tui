// Modified by ZCode: 改为 ZCODE_VENDOR 四字段驱动的厂商配置模板，支持换厂商只改这几个值。
// 本分支全部改动的清单见 README.md「本分支的改动」。
# 当前生效的厂商配置。换厂商只需改这几个值。
ZCODE_VENDOR=bigmodel
ZCODE_VENDOR_MODEL=GLM-5.3
ZCODE_VENDOR_API_KEY=
# 端点选填（自建端点必填）：内置厂商留空即自动使用该厂商的端点。
# 填了就会与该厂商的端点做一致性校验，不一致会报错而不是静默取其一。

# 可用厂商（ZCODE_VENDOR）：
#
# —— Coding Plan 套餐（走加密凭据库）——
#   bigmodel / zai                     个人套餐（默认）
#   bigmodel-team-plan / zai-team-plan
#   bigmodel-start-plan / zai-start-plan
#   bigmodel-offpeak-plan / zai-offpeak-plan
#
# —— 普通 API Key（内联在个人 Provider 配置）——
#   bigmodel-standard / zai-standard
#   moonshot（别名 kimi）  minimax  deepseek
#   qwen（别名 qwen-cn）   qwen-intl  xiaomi
#   openai  anthropic  xai  openrouter
#   opencode-go-chat / opencode-go-messages / opencode-go-responses
#   opencode-zen-chat / opencode-zen-messages / opencode-zen-responses
#
# 自建端点：留空 ZCODE_VENDOR，只填 ZCODE_VENDOR_BASE_URL 即可。

# ── 个人配置（skill / 指令 / 命令 / MCP）────────────────────────────
# 用户级配置家目录，默认 ~/.claude。留空即用默认值。
# 一般不需要设置；仅在需要隔离多套配置时才改。
# 注：这只影响「配置」，不影响会话、凭据等运行时数据（那些仍在 ~/.zcode）。
# ZCODE_CONFIG_HOME=

# 设为 1 可关闭「~/.claude 不存在时自动创建并写入说明文件」的行为（默认开启）。
# ZCODE_NO_CONFIG_HOME_BOOTSTRAP=
